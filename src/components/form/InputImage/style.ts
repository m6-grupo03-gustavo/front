import styled from 'styled-components'

export const StyleFieldsetImage =  styled.fieldset`
    position: relative;
    width: 28rem;
    padding: 1rem;
   
    input{

        width: 88%;
        padding: 1.5rem;
        background: var(--background);
        border-radius: 5px;
        outline: none;
        color: var(--grey0);
        font-size:var(--font-size-5);
        border: 1px solid var(--border);
    }

`